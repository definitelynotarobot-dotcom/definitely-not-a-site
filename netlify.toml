[build]

# Step 1: Run npm ci to install all Node dependencies (including sass-embedded).
# Step 2: Run the Hugo build using 'npx' to ensure Hugo correctly finds and utilizes 
# the locally installed Node executables (like the Dart Sass compiler).
command = "npm ci && npx hugo --gc --minify"

# Hugo output directory
publish = "public"